import fs from 'fs-extra'

export default path => {
  const exists = fs.existsSync(path)

  if (exists) {
    return fs.readJsonSync(path)
  }

  console.warn(`${path} does not exist and cannot be read.`)

  return undefined
}
